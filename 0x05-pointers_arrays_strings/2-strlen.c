#include "main.h"

/**
 * _strlen - return the lenth of the string as an int value
 * @s: parameter
 *
 * Retrun: Int value of the length
 */
int _strlen(char *s)
{
	int l = 0;

	while (*s++)
		l++;
	return (1);
}
